# DMI Review Workflow

## 역할

이 저장소는 예측을 생성하지 않는다. 두 독립 에이전트가 이미 저장한 결과를 장 마감 후 평가하고 비교한다.

## 실행 입력

1. `/config/SOURCES.md`를 읽어 두 입력 저장소를 확인한다.
2. `/prompt/REVIEW_PROMPT.md`를 읽는다.
3. 자동화가 지정한 `DATE`와 `PREDICTION_RUN_TIMES_KST`를 확정한다. 일일 리뷰의 슬롯 목록은 [03:30, 08:30]이며 REVIEW_TIME_KST=16:30이다. 단일 슬롯 수동 리뷰는 기존 PREDICTION_RUN_TIME_KST도 허용한다.
4. 각 저장소에서 해당 날짜와 지정된 슬롯들의 결과 파일만 읽는다.
5. KRX 정규장 Ground Truth를 조사해 리뷰를 완료한다.
6. `/templates/REVIEW_OUTPUT.md` 형식으로 저장한다.

슬롯 목록과 단일 슬롯이 모두 지정되지 않았다면 임의로 실행 결과를 고르지 않고 실패 이유를 보고한다.

## 입력 선택

기본 파일:
`runs/YYYY-MM-DD/HHMM.md`

각 에이전트·슬롯을 별도로 처리한다. 해당 슬롯 기본 파일과 rerun 파일의 경로·SHA·최초 저장 commit 시각을 확인하고, 시간순으로 구조·날짜·슬롯·cutoff와 본문/캡슐 일치를 검증한다. 유효성과 장전 저장을 충족하는 가장 이른 결과 하나만 공식 입력으로 선택한다. 최신 rerun으로 이미 유효한 예측을 교체하지 않는다.
- 최초 저장시각은 GitHub 이력으로 확인한다. 본문의 자기신고 시각이나 마지막 수정시각만으로 장전임을 인정하지 않는다. 원본 이후 내용이 수정되었으면 최초 저장 commit의 내용을 검증한다.
- 해당 DATE의 실제 KRX 정규장 개장 전에 저장된 결과만 장전 공식 비교에 포함한다. 개장시각은 공식 자료로 확인한다.
- 장후 저장은 LATE, 최초 저장시각 확인 불가는 TIMING_UNVERIFIED로 분리하고 공식 KPI에서 제외한다. 후보의 사후 관찰값은 별도로 설명할 수 있다.
- 후보/순위/예상값이 변하는 재분석과 단순 형식 복구를 혼동하지 않는다. 최초 유효 입력 이후의 복구·재분석은 공식 입력을 대체하지 않는다.
- 파일이 없거나 유효하지 않으면 해당 에이전트·슬롯을 UNAVAILABLE로 기록한다. 누락 결과를 복원하거나 추정하지 않는다.
- 선택·제외한 경로, commit, 최초 저장시각, 채택/제외 사유를 본문에 기록한다.

리뷰는 에이전트 저장소에서 다음만 읽는다.
- 지정된 DATE·슬롯의 입력 선택 검증에 필요한 결과와 해당 파일의 저장 이력. 공식 비교에는 선택한 하나만 사용한다.

읽지 않는 대상:
- 에이전트 프롬프트
- 에이전트 WORKFLOW
- 다른 날짜 또는 지정된 슬롯 목록 밖의 결과
- 현재 저장소의 `legacy/**`

## 일일 비교와 시각

03:30의 Agent 1 vs Agent 2, 08:30의 Agent 1 vs Agent 2를 각각 계산한다. 같은 에이전트의 03:30 vs 08:30 비교도 별도로 작성하며 후보를 합쳐 하나의 TOP으로 만들지 않는다. 08:30은 새 정보 분석이지 오류 복구가 아니다. 누락된 그룹은 N/A와 사유를 남기고 존재하는 그룹은 계속 리뷰한다.
DATE는 대상 거래일, REVIEW_TIME_KST는 예약 슬롯 16:30이다. 실제 시작과 문서 완성 시각을 별도로 기록한다. 공식 KRX 휴장일은 SKIPPED_NON_TRADING_DAY로 종료한다. 확정 OHLC가 아직 없으면 추정하지 않고 해당 항목을 N/A로 남긴다.

## 쓰기 범위

리뷰는 `totjae/dmi-market-pipeline`에만 쓴다. 에이전트 저장소의 프롬프트·결과를 수정하지 않는다.

정상 경로:
`reviews/YYYY-MM-DD/review_HHMM.md`

HHMM은 리뷰 슬롯이므로 일일 리뷰는 review_1630.md이다. 두 예측 슬롯을 한 문서의 서로 다른 결과 블록에 보존한다.

기존 파일이 있으면 `review_HHMM_rerun_01.md`부터 번호를 증가시키며 덮어쓰지 않는다.

저장 후 metadata, report, result wrapper와 입력 경로, 후보 수, 계산값을 재검증한다. 실제 저장과 검증이 성공한 경우에만 `SUCCESS`로 보고한다.
